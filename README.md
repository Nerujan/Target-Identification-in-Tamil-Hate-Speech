# Tamil Hate Speech Target Identification

This project focuses on identifying the **target of hate speech in Tamil text** using the transformer model. The system detects hate speech and classifies the target as an **individual, community, or organization**. The model is trained and evaluated using annotated Tamil hate speech datasets and standard NLP metrics such as **F1-score**.
